apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shared-pvc-with-blackjack-and-hookers
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: manual
  resources:
    requests:
      storage: 1Gi
